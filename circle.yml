machine:
  node:
    version: 0.10.30

test:
  post:
    - env
  override:
    - ./node_modules/grunt-cli/bin/grunt test

deployment:
  production:
    branch: master
    command: node ./scripts/deploy.js
