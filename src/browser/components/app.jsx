/** @jsx React.DOM */

var React = require('react'),
    Content = require('./content/content.jsx');

module.exports = React.createClass({
    render: function() {
        return (
            <Content />
        );
    }
});

